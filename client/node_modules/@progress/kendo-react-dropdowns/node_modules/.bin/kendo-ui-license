#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../kendo-licensing/bin/kendo-ui-license.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../kendo-licensing/bin/kendo-ui-license.js" "$@"
  ret=$?
fi
exit $ret
