import { Variant } from "framer-motion";
export declare type MotionVariants<T extends string> = Record<T, Variant>;
export declare const EASINGS: {
    ease: number[];
    easeIn: number[];
    easeOut: number[];
    easeInOut: number[];
};
