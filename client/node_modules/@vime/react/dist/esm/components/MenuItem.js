import { createComponent } from '../lib';
import { VmMenuItem, VmIcon } from '@vime/core';
import { define } from '../lib';
define('vm-menu-item', VmMenuItem);
define('vm-icon', VmIcon);
;
export default createComponent('vm-menu-item', new Set(['identifier', 'hidden', 'label', 'menu', 'expanded', 'checked', 'hint', 'badge', 'checkIcon', 'icons', 'isTouch']));
//# sourceMappingURL=MenuItem.js.map