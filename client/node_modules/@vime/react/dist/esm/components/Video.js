import { createComponent } from '../lib';
import { VmVideo, VmFile } from '@vime/core';
import { define } from '../lib';
define('vm-video', VmVideo);
define('vm-file', VmFile);
;
export default createComponent('vm-video', new Set(['willAttach', 'hasCustomTextManager', 'crossOrigin', 'preload', 'poster', 'controlsList', 'autoPiP', 'disablePiP', 'disableRemotePlayback', 'mediaTitle']));
//# sourceMappingURL=Video.js.map