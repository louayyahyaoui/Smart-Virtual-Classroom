import { createComponent } from '../lib';
import { VmUi } from '@vime/core';
import { define } from '../lib';
define('vm-ui', VmUi);
;
export default createComponent('vm-ui', new Set(['isVideoView', 'playsinline', 'isFullscreenActive']));
//# sourceMappingURL=Ui.js.map