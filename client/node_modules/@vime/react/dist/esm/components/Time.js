import { createComponent } from '../lib';
import { VmTime } from '@vime/core';
import { define } from '../lib';
define('vm-time', VmTime);
;
export default createComponent('vm-time', new Set(['label', 'seconds', 'alwaysShowHours']));
//# sourceMappingURL=Time.js.map