'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-9e4d61ac.js');
const withComponentRegistry = require('./withComponentRegistry-50d191fe.js');
const withPlayerContext = require('./withPlayerContext-460e142f.js');
require('./PlayerEvents-58225f02.js');
const IconRegistry = require('./IconRegistry-94bb5166.js');

const IconLibrary = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
    /** @internal */
    this.icons = 'material';
    withComponentRegistry.withComponentRegistry(this);
    withPlayerContext.withPlayerContext(this, ['icons']);
  }
  handleUpdate() {
    this.register();
  }
  connectedCallback() {
    this.register();
  }
  disconnectedCallback() {
    if (!withComponentRegistry.isUndefined(this.name))
      IconRegistry.deregisterIconLibrary(this.name);
  }
  register() {
    IconRegistry.registerIconLibrary(this.name ?? this.icons, this.name ? this.resolver : undefined);
  }
  get host() { return index.getElement(this); }
  static get watchers() { return {
    "name": ["handleUpdate"],
    "resolver": ["handleUpdate"],
    "icons": ["handleUpdate"]
  }; }
};

exports.vm_icon_library = IconLibrary;
