import { r as registerInstance, g as getElement } from './index-d8f97135.js';
import { w as withComponentRegistry, o as isUndefined } from './withComponentRegistry-892f231d.js';
import { w as withPlayerContext } from './withPlayerContext-9c30712f.js';
import './PlayerEvents-c74733e6.js';
import { d as deregisterIconLibrary, r as registerIconLibrary } from './IconRegistry-0168f7a6.js';

const IconLibrary = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
    /** @internal */
    this.icons = 'material';
    withComponentRegistry(this);
    withPlayerContext(this, ['icons']);
  }
  handleUpdate() {
    this.register();
  }
  connectedCallback() {
    this.register();
  }
  disconnectedCallback() {
    if (!isUndefined(this.name))
      deregisterIconLibrary(this.name);
  }
  register() {
    registerIconLibrary(this.name ?? this.icons, this.name ? this.resolver : undefined);
  }
  get host() { return getElement(this); }
  static get watchers() { return {
    "name": ["handleUpdate"],
    "resolver": ["handleUpdate"],
    "icons": ["handleUpdate"]
  }; }
};

export { IconLibrary as vm_icon_library };
